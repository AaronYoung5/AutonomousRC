#pragma once

#include "Common.h"

class SerialHandler {
public:
  // Public class variables
  enum MessageType { STEERING, THROTTLE, BRAKING };

private:
  // Private class variables

public:
  // Public methods
    SerialHandler();

  private:
    // Private methods
};
